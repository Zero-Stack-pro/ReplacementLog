# 🔒 Исправление утечки Telegram Bot Token

## Проблема
Telegram Bot Token был обнаружен в публичном репозитории GitHub через GitGuardian.

## Что сделано

1. ✅ **Токен удален из всех .md файлов** - заменен на placeholder `YOUR_TELEGRAM_BOT_TOKEN_HERE`
2. ✅ **Изменения закоммичены и запушены** в GitHub
3. ⚠️ **Токен все еще в истории git** - требуется очистка истории

## КРИТИЧЕСКИ ВАЖНО: Создать новый токен!

Старый токен скомпрометирован и должен быть отозван:

1. **Отзовите старый токен:**
   - Откройте @BotFather в Telegram
   - Отправьте `/revoke`
   - Выберите вашего бота
   - Подтвердите отзыв токена

2. **Создайте новый токен:**
   - Откройте @BotFather в Telegram
   - Отправьте `/mybots`
   - Выберите вашего бота
   - Выберите "API Token"
   - Скопируйте новый токен

3. **Обновите токен на сервере:**
   ```bash
   ssh zero@10.45.20.40
   nano ~/ReplacementLog/.env
   # Замените старый токен на новый
   sudo systemctl restart replacementlog
   ```

## Очистка истории git (опционально, но рекомендуется)

Для полного удаления токена из истории git:

```bash
# Установите git-filter-repo (если еще не установлен)
pip install git-filter-repo

# Удалите токен из всей истории
git filter-repo --replace-text <(echo "8391231295:AAE1UNo_b3IH1CB29ktSAYIM41s1dg8oFrQ==>YOUR_TELEGRAM_BOT_TOKEN_HERE")

# Force push (ОСТОРОЖНО! Это перезапишет историю)
git push origin --force --all
```

**Внимание:** Force push перезапишет историю на GitHub. Убедитесь, что все участники проекта синхронизированы.

## Альтернативный способ (без force push)

Если не хотите перезаписывать историю, просто:
1. Создайте новый токен
2. Обновите его на сервере
3. Старый токен больше не будет работать после отзыва

## Проверка

После обновления токена проверьте:

```bash
ssh zero@10.45.20.40
cd ~/ReplacementLog
source .venv/bin/activate
python3 manage.py shell
```

```python
from django.conf import settings
token = getattr(settings, 'TELEGRAM_BOT_TOKEN', None)
print('Токен установлен:', 'да' if token else 'нет')
# Проверьте, что это НОВЫЙ токен, а не старый
```

## Предотвращение в будущем

1. ✅ `.env` файл уже в `.gitignore`
2. ✅ Токен удален из документации
3. ⚠️ Используйте переменные окружения вместо хардкода
4. ⚠️ Проверяйте коммиты перед push (используйте pre-commit hooks)

## Файлы, где был токен (теперь исправлены)

- `TELEGRAM_SETUP.md`
- `QUICK_CHECK.md`
- `QUICK_SYSTEMD_SETUP.md`
- `TELEGRAM_FIXED.md`
- `SERVER_SETUP.md`
- `TELEGRAM_TROUBLESHOOTING.md`
- `DNS_FIX.md`

Все файлы обновлены, токен заменен на placeholder.

